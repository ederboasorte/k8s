apiVersion: v1
kind: Service
metadata:
  name: nginx-headless
  labels:
    app: nginx
spec:
  selector:
      app: nginx
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
  clusterIP: None